import {StyleSheet} from 'react-native';
import {Colors} from 'src/theme';

export default StyleSheet.create({
    image:{
        width:200,
        height:200,
        resizeMode:'contain'
    }
});
