import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  notificationWrapper: {
    height: 70,
    backgroundColor: '#fff',
    padding: 20,
    borderRadius: 10,
    marginBottom: 10,
  },
});
