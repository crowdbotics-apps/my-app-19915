import {StyleSheet} from 'react-native';
import {Colors} from 'src/theme';

export default StyleSheet.create({
  notificationWrapper: {
    height: 70,
    backgroundColor: '#fff',
    padding: 20,
    borderRadius: 10,
    marginBottom:10
  },
});
