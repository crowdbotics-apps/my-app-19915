import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    touch: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    footer: {
        height: 100
    }
});