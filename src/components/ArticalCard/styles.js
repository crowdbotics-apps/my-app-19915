import {StyleSheet} from 'react-native';
import {Colors} from 'src/theme';

export default StyleSheet.create({
  imageWrapper: {
    borderRadius: 10,
    height: 100,
  },
  card: {
    width: 158,
  },
  textWrapper: {
    height: 44,
  },
});
